<h1 class="text-2xl font-semibold text-slate-900">Record checkpoint</h1>
<p class="mt-1 text-sm text-slate-500">
  An assertion that <strong><%= @account.name %></strong> closed at a given amount on a given date.
  Its balance becomes this number plus everything dated after it.
</p>
<div class="mt-6 max-w-xl rounded bg-white p-6 shadow">
  <%= render "form" %>
</div>
